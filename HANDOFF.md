# 오행 — 사주 · 타로 앱 HANDOFF

> 다음 세션에서 이어서 작업할 때 가장 먼저 읽어야 하는 문서.

최종 업데이트: 2026-08-20 (라운드 10)

## ⚠️ 지금 가장 먼저 볼 것
**라운드 10(타로 카드 선택 3중 수정 · 용신 관법 세 갈래 분리) 배포 완료 (2026-08-20).**
- 사용자 요청 2건. ① "카드 선택 버튼을 눌러도 한 번에 선택이 안 된다" ② Threads 사주 프롬프트 글을 반영해 해설 보충.
- 배포 저장소 `NJ1099/saju-ohaeng` `d149444` → `sajumoya.vercel.app` 반영 확인.
- 검증: 단위 **6,079 PASS** · Playwright UI **154 PASS**(기능 24 + 타로 71 + 지도 42 + **픽 17 신규**) · **프로덕션 스모크 8 PASS**.
- 상세는 아래 "라운드 10" 절.

---

## 이전 이정표
**라운드 9(타로 휠 반응 속도 · 귀인 지도 날짜 UI) 배포 완료 (2026-08-17).**
- 사용자 요청 2건짜리 소규모 라운드. 기능 추가 없음, 조작감·시각 정돈만.
- 배포 저장소 `NJ1099/saju-ohaeng` `a7f8dad` → `sajumoya.vercel.app` 반영 확인.
- 검증: 단위 **6,079 PASS** · Playwright UI **148 PASS** · **프로덕션 스모크 113 PASS**.
- 업데이트 요약을 텔레그램 채널로 발송함(message_id 187).
  ⚠️ **채널 이름이 "코딩으로 이것저것 만드는거 기록하는 방" → "리어카 바이브코딩 기록실" 로 바뀌었다.**
  chat_id 는 그대로라 발송에는 영향 없다. 토큰·chat_id 쌍은 라운드 8 메모와 동일
  (`Seed-ledger-main/.env` 의 `TELEGRAM_NEWS_BOT_TOKEN` + `TELEGRAM_NEWS_CHAT_ID`).
- 상세는 아래 "라운드 9" 절.

**라운드 8(타로 아치 캐러셀 · 이미지 저장 · 귀인 지도 · 생년월일 유형 · 약관) 배포 완료 (2026-08-16).**
- 앱이 **사주 + 타로 → 사주 + 타로 + 귀인 지도 세 갈래**가 됐다. 메뉴 타일 3개.
- 배포 저장소 `NJ1099/saju-ohaeng` `08c6ed1`(+문서 `5cc2c32`) → `sajumoya.vercel.app` 반영 확인.
  모노repo `NJ1099/AI` `89c9d0f` 에도 백업 커밋 **푸시 완료**(라운드 6·7과 달리 이번엔 원격까지 올라갔다).
- 검증: 단위 **6,079 PASS**(만세력 80 + 이론·궁합 273 + 타로 2,886 + 유형·귀인 2,840),
  Playwright UI **148 PASS**(기능 11 + 궁합 24 + 타로 71 + 지도·약관 42).
  배포 후 **프로덕션 스모크 113 PASS**(`BASE=https://sajumoya.vercel.app node tests/verify-{tarot,map}.mjs`).
- 업데이트 요약은 텔레그램 채널("코딩으로 이것저것 만드는거 기록하는 방")에 `seed_rearbot` 으로 발송함.
  ⚠️ 루트 `.env` 의 `myClaudecheck_bot` 은 **그 채널의 멤버가 아니라 발송 실패**한다. 채널 발송은 Seed-ledger 의
  `TELEGRAM_NEWS_BOT_TOKEN` + `TELEGRAM_NEWS_CHAT_ID` 쌍을 쓸 것. (`news-dashboard/.env` 의 `TELEGRAM_CHAT_ID` 는 비어 있음)
- **⚠️ 배포 시 이미지에 `awk` CRLF 변환 금지** — JPG가 깨진다. `assets/tarot/*.jpg` 는 `cp` 로 복사할 것.
- 상세는 아래 "라운드 8" 절.

**라운드 7(타로 모드 신설 + 전체화면 메뉴) 배포 완료 (2026-08-15).**
- 앱이 **사주 전용 → 사주 + 타로 두 갈래**가 됐다. 첫 진입은 전체화면 메뉴(사주 / 타로).
- 라이더-웨이트 78장 · 9주제 × 정/역 = **해석 1,404줄** 전량 집필. 카드 이미지 78장 번들(6.2MB).

**라운드 6(명리 이론 계층 + 둘의 궁합) 배포 완료 (2026-07-30).**
- 라운드 6은 2026-07-29 세션에 코드가 완성됐으나 **문서 갱신·커밋·배포가 안 된 채로 끊겨 있었다.** 2026-07-30 세션에 마무리했다.
- 배포 저장소 `NJ1099/saju-ohaeng` `a5bcad6` → `sajumoya.vercel.app` 반영 확인. 모노repo `NJ1099/AI` `cb8abd6` 에도 백업 커밋.
- **경로 변경 주의** — 구 문서의 `D:\Claude\saju`(개발 원본)·`D:\saju-ohaeng`(배포용 사본)은 **둘 다 존재하지 않는다**(D 드라이브 자체가 없음). 현재 개발 원본은 `E:\AI\saju-ohaeng-main`, 배포용 클론은 이번에 새로 만든 **`E:\saju-sync`** 다.
- 검증 상태(2026-07-30): 단위 353/353 PASS(만세력 80 + 이론·궁합 273), 로컬 Playwright UI 24/24 PASS, **프로덕션 스모크 7/7 PASS**(이론 카드 7블록·격국 표기·궁합 85점·6축 막대·네 기둥 대조표·콘솔 에러 0).
- **줄바꿈 주의** — 배포 저장소는 **CRLF**, 로컬 원본은 **LF** 다. 그냥 복사하면 전 파일이 diff에 잡혀 커밋이 지저분해진다. 복사 시 `awk '{ printf "%s\r\n", $0 }'` 로 CRLF 변환할 것.

## 현재 상태 한 줄 요약
첫 화면에서 **사주 / 타로 / 귀인 지도** 세 갈래를 고른다. 하단 푸터에 **이용약관 · 개인정보처리방침**.
**귀인 지도** — 내 생일로 지도를 만들어 링크를 뿌리면, 친구가 생일을 넣고 "나는 ○○님에게 🌟 귀인" 판정을 받는다. 그 결과 링크를 방장이 열면 지도에 쌓인다(서버 없이 방장 브라우저 localStorage).
**생년월일 유형** — 사주 결과 맨 위에 "🔥 여름 화초형 — 뜨거운 날에도 쉽게 시들지 않는 사람" 같은 40종 유형 카드.
**타로** — 9주제(연애·미래·대인관계·재회·이직·사업·재물·진로·시험) 중 하나를 고르면 주제마다 자리 뜻이 다른 3장 스프레드가 열리고, 검은 배경에 펼쳐진 라이더-웨이트 78장에서 3장을 뽑아 정/역방향까지 반영해 풀이한다. 프롬프트 복사·공유 링크·이미지 저장·용어 팝오버 지원.
**사주** — 이름·생년월일·시간(양/음력)만 입력하면 **만세력으로 사주 원국을 자동 계산**하고 오행·십성·7대 영성지표·**격국·용신·조후·통근 신강약·십이운성·신살·형충회합**·대운/세운을 분석, **쉬운 요약 + 부족한 기운 뜻(+자세히 보기) + 어떤 사주와 잘 맞는지 + 자세한 풀이 + 용어 설명 팝오버**를 보여주고, **두 사람의 궁합(6축 100점)** 까지 산출하며 **GPT·Claude 프롬프트 복사(단독·궁합 전용 2종) / 카톡·텔레그램 공유 / 이미지 저장**이 되는 토스풍 정적 SPA. 단위테스트 353개 전부 통과. 3차원 적대 검증 반영 완료.

## 지금 동작하는 것
- **만세력 자동 변환** — 양력/음력(윤달 포함) 입력 → 사주 4기둥. 절기(VSOP87 태양황경)·신월(Meeus 삭) 천문계산 기반.
- **진태양시 보정** — KST 표준시 역사(1908~현재 4구간) + 경도 보정(서울 −32분) + 균시차 + 서머타임 자동.
- **자시(子時) 경계 학설 토글** — 정자시(23시, 다수설) / 자정(00시).
- **분석** — 오행 분포(글자수+지장간 가중%), 십성, 7대 영성지표(화개·태극귀인·공망·편인·사고·칠살상관·壬癸丁子午卯酉), 스페셜 조합, 영성점수.
- **명리 이론 계층** (`engine/theory.js`, 라운드 6) — 통근(通根)·투간(透干) 기반 신강/신약 정밀 판정(득령·득지·득세, 무근 천간 가중 0.6), 격국(내격 10종), 용신 후보(억부+조후), 조후 한난조습, 십이운성 12단계, 신살 8종(천을귀인·문창·역마·도화·양인·홍염·괴강·백호), 원국 내 형충회합. 학설이 갈리는 항목은 통설을 기본값으로 두고 결과 객체 `note`에 학설차를 명시.
- **둘의 궁합(宮合)** (`engine/compat.js`, 라운드 6) — 상단 세그먼트에서 `둘의 궁합` 선택 시 두 번째 사람 입력 노출. 6축 100점(①일간 25 ②일지 배우자궁 25 ③오행 상호보완 20 ④년지 10 ⑤월지+조후 10 ⑥신강약 균형 10). 총점·축별 막대·항목 카드 6종·두 사람 원국·서로에게 어떤 십성인지(양방향)·네 기둥 대조표·조율 포인트·실천 제안. 궁합 전용 프롬프트 복사 + 공유 링크에 두 사람 입력 인코딩.
- **대운·세운** — 양남음녀 순행/역행, 절기 기반 대운수, 대운 타임라인(현재 대운 하이라이트), 세운 12년 표 + 십성. (`engine/luck.js`)
- **쉬운 요약 카드** — 일간 비유·오행 균형·성향·요즘 흐름·오늘 한 걸음을 쉬운 말로(오행 분포 바로 아래). (`buildSimpleSummary`)
- **부족한 기운 뜻 + 자세히 보기** — 오행별 **뜻**(상징)·쉬운 요약을 먼저 보여주고, 경향·장점·보완법은 '자세히 보기'로 접음. (`buildElementWeakness`)
- **어떤 사주와 잘 맞을까(궁합)** — 부족한 오행을 채워 줄 사람·천간합 인연·나를 북돋는 오행·가벼운 주의·면책. 단정 아닌 방향 안내. (`buildCompatibility`)
- **내장 자동풀이** — 프롬프트 7섹션 구조의 적응형 한국어 내러티브(자세한 풀이, 하단).
- **용어 설명 팝오버** — 밑줄+ⓘ 단어 탭 시 작은 설명창. (`engine/glossary.js`)
- **프롬프트 복사** — 계산된 원국(+대운/세운)을 원본 프롬프트에 채워 클립보드 복사 → GPT·Claude.
- **공유** — `공유하기`: 모바일 OS 공유시트 직행(카톡·텔레그램)·PC 자체 바텀시트. `이미지 저장`: 모바일 `navigator.share(파일)`(갤러리 저장·앱 전송)·PC 다운로드. **공유 링크에 입력값 인코딩 → 링크로 결과 재현.**
- **UI** — 토스풍 반응형(모바일 우선), 전폭 blur 톱바, 파비콘·PWA 메타, 한자 음독 병기, **맨 위로 ↑ 버튼**, 만세력 4기둥·오행 차트·지표 카드·게이지·대운/세운.
- **배포** — GitHub `NJ1099/saju-ohaeng` push → Vercel `sajumoya.vercel.app` 자동 재배포. (`netlify.toml`·`.nojekyll`·`DEPLOY.md`도 포함. 모노레포 통째 배포 금지 — saju만.) 라운드 6까지 반영 완료.

## 최근 라운드에서 한 일

### 라운드 10 — 타로 카드 선택 3중 수정 · 용신 관법 세 갈래 (2026-08-20)

#### ① 타로 "이 카드 뽑기가 한 번에 안 먹는다" — 원인이 셋이었다

추측하지 않고 기기별 재현 테스트를 먼저 짰다. 세 개가 **동시에** 겹쳐 있었다.

| 원인 | 실측 |
|---|---|
| **버튼이 화면 밖으로 잘려 있었다** | iPhone SE **95px 잘림**(버튼 높이 50px → 통째로 화면 밖), iPhone 13 **37px**(위 13px만 보임), Galaxy S9+ 5px. Pixel 7만 멀쩡. 실기기는 주소창·하단 툴바까지 있어 더 심하다 |
| **가운데 카드 탭이 흔들림 8px 에서 죽었다** | 0·4px 는 뽑히는데 8·10·14·20px 는 전부 실패. 손가락 탭은 원래 10px 안팎 흔들린다 |
| **누른 뒤 0.73초 무반응** | 슬롯이 채워지기까지 300+430ms. 그 사이 또 누르면 **엉뚱한 두 번째 카드**가 딸려 나갔다(연타 방어 없음) |

수정:
1. `.t-pickbar` → `position: sticky; bottom: 0` + `env(safe-area-inset-bottom)`.
   조상에 `overflow` 가 걸리면 sticky 가 죽는다 — `.t-deck-wrap`(overflow:hidden)은 **형제**라 무관.
2. **드래그 데드존 `TAP_SLOP = 16px` 신설**. 이게 핵심이다.
   한 장 넘기는 거리가 ≈24px 라 **그 절반인 12px 만 흔들려도 카드가 넘어가** 탭이 취소됐다.
   데드존 안에서는 아치를 붙잡고, **벗어나는 순간을 새 기준점으로 삼아**(`drag.armed`) 카드가 툭 튀지 않게 했다.
   탭/드래그 판정은 이제 거리가 아니라 `armed` 하나로 갈린다.
3. `pickActive()` 가 **누른 즉시 `fillSlot()`** 을 부른다. 잔상은 그 위로 날아간다.
   `state.picking` 420ms 락으로 연타 방어. `fillSlot` 은 이미 찬 자리를 다시 채우지 않는다(등장 애니메이션 재시작 방지).

**회귀 테스트 `tests/verify-pick.mjs` 신설 (17건)** — 4개 기기 × 버튼 노출(+맨아래 스크롤), 흔들림 0·4·8·10·14px, 크게 밀면 안 뽑힘, 즉시 피드백(150ms), 연타 방어, 3장 완주. `npm run test:pick` / `test:ui` 에 편입.

#### ② 사주 — 용신을 억부·조후·격국 **세 갈래로 분리**

사용자가 준 Threads 글(@deep_peace_33, ChatGPT 사주 프롬프트)의 핵심 원칙:
> "억부용신·격국용신·조후용신을 각각 구하고, 서로 다르다면 **억지로 하나로 통일하지 말고** 왜 다른지 설명하라"

**앱은 이 원칙을 어기고 있었다.** `judgeYongsin` 이 억부와 조후를 `primary` 한 배열로 합쳐 내보내
**어느 관법의 결론도 아닌 값**을 화면에 띄우고 있었다.

- **`judgeGyeokgukYongsin()` 신설** — 『자평진전』식 **상신(相神)**. 월지 본기 십성 10종 전부 규칙화.
  길신격(정관·재성·정인·식신)은 **살려 주는** 글자를, 흉신격(칠살·상관·편인·양인)은 **눌러 주는** 글자를 상신으로.
- `judgeYongsin(s, strength, johu, gyeokguk)` 로 시그니처 확장 — 셋을 각각 반환.
  공통분모를 먼저 찾고, 없으면 **둘씩 겹치는 지점**을 찾아 문장으로 알려 준다. 그래도 갈리면 갈린 채로 둔다.
  하위호환 필드(`primary`·`eokbu`·`avoid`·`johuFirst`)는 유지했다 — `compat.js` 가 `primary` 를 쓴다.
- **화면** — 용신 카드를 억부/조후/격국 **3줄 비교**(`.yong-grid`)로 교체.
- **이론 블록 7개 전부에 '쉽게 말하면' 한 줄**(`.th-plain`) 추가. 사용자 요청 "더 이해하기 쉽게".
- **신살이 보조자료**라는 위계를 화면에 명시.
- **`data/theory-prompt-template.txt` + `buildTheoryPrompt()` 신설** — 명리 관법 비교 프롬프트(버튼 `#btn-copy-theory`).
  Threads 글의 18항목·원칙 12개를 반영하되, **앱이 계산한 원국·격국·세 용신을 주입**한다.
  원본 프롬프트의 최대 약점(LLM 이 만세력을 직접 뽑다가 틀림)이 사라진다.
- 용어사전에 **상신·억부·자평진전·궁통보감** 추가, 용신 항목 갱신.
- `THEORY.md` 8장을 8-1(억부)·8-2(조후)·8-3(격국/상신 표)·8-4(갈릴 때)로 재작성.

#### 라운드 10에서 실제로 발목을 잡은 것

- **`app.js` 만 CRLF 다.** 다른 소스는 전부 LF. 여러 줄 문자열 치환 스크립트가 조용히 매칭 0회로 실패한다.
  치환 전에 대상 파일의 개행을 감지해 패턴을 변환할 것(`s.includes('
')`).
  theoryHtml 을 LF 로 갈아 끼웠다가 파일에 LF 84줄이 섞였고, CRLF 로 되돌려 통일했다.
- **Playwright MCP 브라우저는 캐시를 쓴다.** 코드를 고쳐도 옛 `app.js` 가 잡힌다.
  UI 검증은 `tests/*.mjs` 로 **새 브라우저 인스턴스**를 띄워서 할 것.
- **아치의 첫 카드는 `visibility:hidden`** 이다(가장자리). `waitForSelector('.t-card')` 가 타임아웃 난다 → `.t-card.active` 를 기다릴 것.
- **패치 스크립트를 heredoc 안에 템플릿 리터럴로 쓰면 백틱이 충돌**한다. 삽입 코드는 별도 `.txt` 조각 파일로 두고 `readFileSync` 로 읽을 것.
- **Git Bash 의 `/tmp` 를 Node 는 `E:	mp` 로 해석**한다. 스크래치패드 절대경로를 쓸 것.
- 한국어 **조사 처리** — `금(金)` 처럼 한자 괄호로 끝나면 받침 판정이 깨진다. 괄호 앞 오행 이름으로 판정해야 한다.

### 라운드 9 — 타로 휠 반응 속도 · 귀인 지도 날짜 UI (2026-08-17)
사용자 요청 2건. ① "카드 휠 UI를 조금만 더 빠르게 돌아가게" ② "귀인찾기 날짜 고르는 UI가 주변 디자인과 자연스럽지 않다".

1. **[튜닝] 타로 아치 회전이 빨라졌다** (`tarot.js` · `styles/tarot.css`)
   - 전환 **0.34s → 0.19s**. 감도만 올리고 전환을 그대로 두면 연속으로 굴릴 때 카드가 손을 못 따라와
     오히려 더 느리게 느껴진다 — 둘을 같이 줄여야 한다.
   - 휠 임계 `WHEEL_PX` **38 → 22** (마우스 한 노치 100px 기준 2장 → 4장). 더 낮추면 트랙패드에서 통제가 안 된다.
   - 드래그 `DRAG_GAIN` **0.62** 신설 — 호 길이 그대로면 78장 훑는 데 손이 너무 많이 간다.
   - `PageUp`/`PageDown` 5장 → 8장. `prefers-reduced-motion` 은 기존대로 전환 off.
2. **[수정] 귀인 지도 날짜·시간 선택** (`map.js` · `styles/app.css`)
   - **원인은 클래스 누락이었다.** 사주 폼은 `class="input select"` 인데 `map.js` 만 `class="select"` 라
     박스 스타일(테두리·패딩·배경)이 통째로 빠졌고, `.select` 의 `appearance:none` 때문에
     **브라우저 기본 각진 테두리만 남아** 같은 카드 안의 이름 입력·세그먼트와 확연히 겉돌았다.
   - 년·월·일을 하나의 둥근 박스 **`.gm-picker`** 로 묶었다. 테두리는 래퍼가 한 번만 그리고 칸 사이는
     얇은 선 — 바로 위 세그먼트 컨트롤과 같은 '한 덩어리' 결. `:focus-within` 파란 테두리 + 링.
   - 윤달 체크박스를 둥근 커스텀 체크로 교체(`appearance:none` + SVG 체크).
   - **'시간 미상'이면 분 칸을 잠근다** — `readForm` 이 이미 `hour===''` 일 때 분을 버리고 있었는데
     고를 수 있게 열려 있어 오해를 줬다(`bindMinuteToggle`).
   - `.gm-row` 는 이 라운드에서 사라졌다(`.gm-picker` 가 대체).

### 라운드 8 — 타로 아치 캐러셀 · 이미지 저장 · 귀인 지도 · 생년월일 유형 · 약관 (2026-08-15)
사용자 요청 3건이 한 세션에 들어왔다.
① "타로 카드 이미지가 작아 옆 카드를 고르게 된다 — 무지개처럼 반쯤 보이는 카드 뭉치, 스크롤로 이동, 번호 카운트, 가운데서 크게, 빠지는 애니메이션"
② "이미지 저장이 모바일에서 안 된다(아이폰·갤럭시 모두 되게)"
③ "귀인 지도를 넣고, 개인정보처리방침·이용약관도 넣고, 생년월일 해석도 추가"

1. **[교체] 타로 덱 = 무지개 아치 캐러셀** (`tarot.js` · `styles/tarot.css`)
   - 라운드 7의 6줄 × 13장 격자를 버리고, 78장을 **위로 볼록한 원호** 위에 놓았다. 양끝은 화면 밖으로 흘러 반쯤만 보인다.
   - 휠·드래그(가로/세로 모두)·좌우 방향키로 아치가 돌고, **꼭대기 한 장만 활성**이다. 활성 카드는 1.52배(약 138×233px).
   - **옆 카드를 눌러도 뽑히지 않고 가운데로 굴러온다** — 오선택이 구조적으로 불가능해졌다. 뽑기는 가운데 카드 재탭 또는 `이 카드 뽑기` 버튼.
   - 카드마다 번호를 달았고 하단에 `N번째 카드 / 남은 M장` 카운터를 뒀다.
   - 뽑는 순간 카드가 위로 솟았다가 슬롯으로 날아가는 **FLIP 잔상(`.t-ghost`)**. 뽑힌 카드는 아치에서 빠지고 뒤 번호가 당겨진다.
2. **[수정] 이미지 저장** (`share.js` `deliverImage()` · `app.js` · `tarot.js`)
   - 원인 ①: **iOS Safari 는 사용자 제스처 직후에만 `share()` 를 연다.** 캔버스를 그리고 폰트·카드 이미지를 기다린 뒤 부르면 활성화가 만료돼 조용히 죽는다. → 결과 화면이 뜨는 순간 **blob 을 미리 만들어 둔다(프리워밍)**. 클릭 시엔 대기 시간이 사실상 0.
   - 원인 ②: **iOS 는 `a[download]` 를 자주 무시한다.** → 3단 폴백: 공유 시트 → `a[download]`(DOM 에 붙여서 클릭) → **이미지를 띄우고 길게 눌러 저장**(`.img-sheet`, 아이폰에서 항상 통함).
   - 파일명을 ASCII 로 바꿨다(`ohaeng-saju-19920708.png`) — 일부 안드로이드 브라우저가 한글 파일명 저장에서 실패한다.
   - `canvasToBlob` 에 `toDataURL` 폴백 추가(구형 iOS 에서 `toBlob` 이 null 을 준다).
3. **[신규] 생년월일 유형** (`engine/persona.js`) — 일간 물상 10 × 계절 4 = **40조합**. "여름 화초형 — 뜨거운 날에도 쉽게 시들지 않는 사람". 사주 결과 맨 위 카드. 근거는 `THEORY.md` 12장.
4. **[신규] 귀인 지도** (`engine/gwiin.js` · `map.js`) — 다섯 유형(🌟귀인 🤝단짝 🌱내 사람 🧭오른팔 ⚡호랑이 선생).
   - **서버 없이** 만든다: 방장이 링크 생성(`#/map?h=…`) → 친구가 생일 입력 → 판정 → `결과 보내기` 링크(`#/map?h=…&g=…`) → 방장이 열면 **localStorage 에 누적**.
   - 판정은 십성이 기준선(14점), 오행 보완·일지 합충·천을귀인이 보정치. 근거는 `THEORY.md` 13장.
5. **[신규] 이용약관 · 개인정보처리방침** (`#/terms` · `#/privacy` 라우트 + 전역 푸터)
   - **개인이 운영하는 비영리 서비스**로 명시. 사업자 정보 없음, 문의는 kiwoong0219@gmail.com.
   - 사실관계 그대로 적었다 — 서버 없음 / 입력값 브라우저 내 처리 / 공유 링크 URL 에 생년월일이 담김 / localStorage 사용 / 외부 통신은 Vercel·jsDelivr 둘뿐.
6. **[수정] 죽어 있던 회귀 테스트 2건** — `verify-features.mjs`·`verify-compat.mjs` 가 라운드 7의 메뉴 도입 이후 `#/saju` 로 안 가서 **전부 실패 중이었다**(라운드 7 기록의 "기존 35 PASS"는 사실이 아니었다). 진입 URL 을 해시 라우트로 고쳤다.

#### 라운드 8에서 실제로 발목을 잡은 것
- **카드 버튼을 카드 그림 크기로 잡으면 이웃이 클릭을 가로챈다.** 아치에서도 같은 문제가 재현됐다(Playwright `intercepts pointer events`). 버튼은 **실제 노출 폭(`--hit-w` = 카드폭 × 0.46)** 만 차지하고, 카드 그림(`> i`)이 그보다 넓게 삐져나오되 `pointer-events:none`. 활성 카드만 `::before` 로 히트 박스를 1.7배 넓힌다(이웃 중심선은 침범하지 않는 선까지).
- **회전한 요소의 `getBoundingClientRect()` 는 축정렬 박스라 실제보다 넓다.** "활성이 이웃보다 크다"를 rect 로 재면 138 vs 133 으로 거의 같게 나온다. 배율은 `--s` CSS 변수로 재야 한다.
- **귀인 유형이 45%까지 '귀인'으로 몰렸다.** 오행 보완 초과분을 그대로 점수로 썼기 때문. 구간으로 끊고(최대 9점), 천을귀인을 일지 하나로 좁히고(3자리 다 보면 42% 적중), 동점 시 십성 기준선이 이기도록 고쳐 27/23/20/16/13% 로 폈다.
- **인코딩을 이중으로 걸면 "내 지도인데 남의 지도"가 된다.** `encodePerson` 이 `encodeURIComponent` 를 하고 URLSearchParams 가 또 인코딩해, 저장된 방장 값과 링크에서 읽은 값이 달라졌다. 인코딩은 URL 조립/파싱 계층에만 맡긴다.
- **번호를 카드 중앙에 두면 이웃 카드에 다 가린다.** 아치에서는 중앙에 가까운 카드가 위에 쌓이므로, 번호를 **바깥쪽 가장자리(`--nx: ±32%`)** 로 밀어야 여러 장의 번호가 함께 읽힌다.

### 라운드 7 — 타로 모드 신설 + 전체화면 메뉴 (2026-08-15)
사용자 요청: "사주 말고 타로도 추가 · 카드 고르는 애니메이션 · 연애/미래/대인관계/재회/이직/사업/재물/진로/시험 등 여러 운 · 첫 화면은 사주·타로 두 버튼만 · 타로는 검은 배경에 카드 여러 장 중 3장 뽑기".

1. **[신규] `engine/tarot.js`** — 순수 로직(DOM·fetch 무의존). `buildDeck` / `indexDeck` / `shuffle`(Fisher–Yates, `crypto.getRandomValues`) / `drawSpread` / `readSpread` / `encodeDraw` / `decodeDraw` / `summarize`.
   - 카드 ID는 URL에 그대로 실리므로 짧게: 메이저 `m00~m21`, 완드 `w01~14`, 컵 `c01~14`, 소드 `s01~14`, 펜타클 `p01~14` + 방향 `u`/`r`.
   - `summarize`는 **세 장을 함께 놓아야 보이는 것만** 담는다(메이저 비중·수트 쏠림·역방향 개수).
2. **[신규] 해석 데이터 `data/tarot/{major,wands,cups,swords,pents}.json`** — 78장 × (키워드 정/역 + 코어 정/역 + **9주제 × 정/역**). 주제 해석만 **1,404줄**.
   - 전거: A.E.Waite, *The Pictorial Key to the Tarot*(1910, 퍼블릭 도메인). 한국어 재서술.
   - **사용자가 준 tistory 링크는 목차 페이지일 뿐 해석 본문이 없다**(실측 확인). 참고 대상에서 제외.
   - 톤 규약은 `compat.js` 계승 — 역방향을 "나쁨"으로 쓰지 않고(안으로 향한 기운/지연/재검토), 숙명론·공포 조장 금지.
3. **[신규] `data/tarot-topics.json`** — 9주제 × 3자리. **자리 뜻이 주제마다 다르다**(연애=내 마음/상대 마음/흐름, 시험=현재 실력/장애물/결과의 결 …). 27개 자리 라벨이 전부 유니크한지 테스트가 강제한다.
4. **[신규] 라우터** `router.js` — 해시 라우팅(`#/`, `#/saju`, `#/tarot`) + `body[data-route]`로 테마 스코프. 정적 호스팅이라 서버 리라이트 없이 새로고침·뒤로가기가 동작한다.
5. **[신규] `tarot.js` + `styles/tarot.css`** — 주제 9칩 → 78장 펼침 → 3장 선택 → 순차 flip 공개 → 해석.
6. **[신규] 이미지 78장** `assets/tarot/*.jpg`(6.2MB) + 재현용 `scripts/fetch-tarot-images.mjs`.
7. **부가 기능 4종** — 타로 전용 LLM 프롬프트(`buildTarotPrompt` + `data/tarot-prompt-template.txt`) / 공유 링크(`?t=주제&c=m00u,w03r,s10u`) / 스프레드 카드뉴스 이미지(`share.js` `drawTarotCard`) / 용어 팝오버(타로 용어 8개 추가).
8. **검증** — `tests/tarot.test.mjs` **2,886건**(덱 무결성·이미지 실존·1,404줄 전량 존재·정역 문장 동일 금지·드로우 1,000회 반복 분포·URL 왕복 200회·손상 입력 10종 방어), `tests/verify-tarot.mjs` **56건**(Playwright).

#### 라운드 7에서 실제로 발목을 잡은 것 (다시 만나면 시간 아끼려고 기록)
- **위키미디어 이미지는 미리 만들어 둔 "버킷 크기"만 준다.** `upload.wikimedia.org/.../<W>px-<name>` 에서 **330·250은 200, 300·320·400은 400(Bad Request)**. 임의 폭을 쓰면 안 된다. 또 `Special:FilePath` 는 앱 서버를 거쳐 연속 요청 시 **429**로 막히므로, CDN 썸네일 경로(파일명 MD5로 `thumb/<m0>/<m0m1>/…`)를 직접 쳐야 한다. 스크립트에 폴백 체인(CDN 330 → CDN 250 → Special:FilePath)을 넣어 뒀다.
- **78장을 한 줄 부채로 펴면 클릭이 안 된다.** 카드 한 장의 노출 폭이 5px 남짓이라 뒤 카드가 앞 카드의 클릭을 전부 가로챈다(Playwright가 `intercepts pointer events`로 잡아냄). → **6줄 × 13장 격자형 부채**로 바꾸고, **버튼(`.t-card`)은 실제 노출 영역만 차지하고 카드 그림(`> i`)은 `pointer-events:none`** 으로 삐져나오게 했다. 이 구조를 깨면 클릭이 다시 망가진다.
- **`tarot.js` 는 `app.js` 보다 늦게 로드된다.** `app.js` 의 `initRouter()` 가 초기 라우트를 이미 적용한 뒤라 `onRoute` 첫 이벤트를 놓친다 → 공유 링크 진입이 안 됐다. `currentRoute() === 'tarot'` 이면 `queueMicrotask` 로 직접 태운다.
- **용어 팝오버가 열리자마자 닫혔다** — 탭 직후의 관성 스크롤이 `scroll → hideTerm` 을 때린다. 여는 순간부터 350ms 동안은 스크롤 무시(`termOpenedAt`). 사주 쪽에도 같이 적용됨.
- **`server.js` MIME 에 `.jpg` 가 없었다** — 추가함(없으면 `octet-stream` 으로 나간다).

### 라운드 6 — 명리 이론 계층 + 둘의 궁합 (2026-07-29 구현 / 2026-07-30 배포)
앱이 "오행 분포 + 영성 지표"에 머물러 있어 명리(命理) 본류 판정이 빠져 있던 것을 채우고, 두 사람 궁합을 신규 기능으로 붙인 라운드.

1. **[신규] `engine/theory.js`** — 이론 판정 11개 함수. `spread`(자리별 전개) → `findRoots`(통근) → `judgeStrength`(신강약) → `twelveStages`(십이운성) → `findSinsal`(신살 8종) → `judgeJohu`(조후) → `judgeGyeokguk`(격국) → `judgeYongsin`(용신) → `branchRelation`/`findInternalRelations`(형충회합) → `analyzeTheory`(집계).
   - **신강약이 오행 글자수 근사 → 통근·투간 기반으로 교체됨.** 득령은 월지 **본기**만 인정(중기·여기 통근은 불인정), 지지는 지장간 일수 비례 × 자리 가중(월지 2.4 / 일지 1.4 / 년·시지 1.0), 무근 천간은 0.6으로 감쇄, 일간 자신은 세력에서 제외. 4단계 라벨(신강/중화신강/중화신약/신약)에 득령·득지 동시 실패 시 한 단계 강등.
   - **의도적 제외** — 음간의 양인(乙→寅설 vs 乙→辰설 대립), 종격·화격 등 외격. `note`에 명시.
2. **[신규] `engine/compat.js`** — `analyzeCompatibility(A, B)` 6축 100점. 설계상 함정 3가지를 피해 갔으므로 수정 시 주의:
   - 관계 강도는 **축의 점수 폭에 비례**해 가감한다(고정 점수를 쓰면 배점 10점짜리 년지 축에서 관계 하나가 과대 반영됨). 육합 1.0·삼합 0.8·방합 0.5 / 충 1.0·원진 0.85·형 0.7·해 0.55·파 0.5.
   - 오행 보완은 **초과분** `max(0, 상대% − 내%)`(`excessOf`)로 잰다. 절대 보유량으로 재면 **같은 사주끼리도 서로를 채워 주는 것으로 잘못 나온다**.
   - 용신 교류도 같은 이유로 초과분 기준. 절대량 합을 쓰면 용신 후보 2~3개일 때 거의 항상 만점.
   - 톤 규약: "나쁜 궁합" 표현 금지, 낮은 점수는 "결이 다름 → 조율 필요", 충·형은 '위험'이 아니라 '자극과 변화의 힘', 한쪽을 우위/고쳐야 할 사람으로 그리지 않음.
3. **UI/연동** — `index.html` 모드 세그먼트(`혼자 보기` / `둘의 궁합`) + 두 번째 사람 입력 블록. `app.js` `renderCouple`/`compatScoreHtml`/`compatBarsHtml`/`compatHtml` 신규, `buildShareUrl(inputs, mode)`가 궁합 모드 인코딩 지원. `engine/constants.js` 테이블 확장(십이운성 장생지·신살·형충회합·지장간 일수), `engine/glossary.js` 용어 추가(격국·용신·조후·통근 등), `engine/promptBuilder.js` + `data/compat-prompt-template.txt` 로 **궁합 전용 프롬프트** 신설, `engine/reading.js`·`styles/app.css` 확장.
4. **문서** — `THEORY.md` 신규(채택 학설·근거·의도적 제외 항목·참고 문헌 10장), `README.md` 전면 개편, `data/theory-reference.json`(검증 벡터 6종: unseong·sinsal·relations·johu·gyeokguk·compat).
5. **검증** — `tests/theory.test.mjs` **273건 신규**(십이운성 10간×12지 순환 완전성 + 건록지 검산, 신살 테이블 전건 대조, 형충회합 전 쌍 + 오탐 방지[戊己 무충·비자형 8지지], 궁합 배점 합계 100·축별 범위·좌우 대칭성·시주 미상 처리·780쌍 분포 무결성). `tests/verify-compat.mjs` **24건 신규**(Playwright). `package.json` 에 `test:theory`/`test:ui` 스크립트 추가.
   - 궁합 점수 분포(60명×1,770쌍): `min 47 · p25 65 · 중앙 70 · p75 76 · max 94`.

### 라운드 5 — 맨위로 버튼 픽스 · 부족한 기운 뜻+자세히보기 · 잘 맞는 사주(궁합 힌트) (2026-06-19)
1. **[버그픽스] 맨 위로 ↑ 버튼** — 라운드4에서 추가했으나 동작 안 함. 원인: `<button id="to-top" hidden>`의 HTML `hidden` 속성이 UA `display:none`을 걸어 영구 비표시였고, JS는 `.show` 클래스만 토글(opacity)할 뿐 `hidden`을 안 풀었음. → `index.html`에서 `hidden` 제거(CSS `opacity:0; pointer-events:none` 베이스라인이 스크롤 전 숨김을 담당, `.show`가 노출). 우하단 노출·클릭 시 smooth 최상단·최상단서 재숨김 모두 검증.
2. **[UX] 부족한 기운 — 뜻 먼저 + '자세히 보기'** — 결과가 길어 가독성↓. 각 오행 카드를 ①헤더 ②**뜻**(상징 핵심 3키워드, 파란 뱃지) ③한 줄 쉬운 요약(`brief`) 까지 항상 노출하고, 경향·장점·보완법은 `<details> 자세히 보기`로 접음(기본 접힘). `engine/reading.js`의 `ELEMENT_DETAIL`에 `brief` 추가, `app.js` `weaknessHtml` 재구성, CSS `.el-card`/`.el-more` 신설(구 `.el-detail` 아코디언 대체).
3. **[신규] 어떤 사주와 잘 맞을까(궁합)** — `engine/reading.js` `buildCompatibility(saju,a)` + `COMPAT` 상수. ①내게 부족한 오행을 가진 사람(보완) ②**천간합 인연**(STEM_HAP: 갑기합토·을경합금·병신합수·정임합목·무계합화) ③나를 생(生)하는 오행이 강한 사람(북돋움, 보완과 겹치면 생략) ④같은 강한 오행끼리의 가벼운 주의 ⑤두 사주를 함께 봐야 한다는 면책. `app.js` `compatHtml`로 **보완 렌즈 vs 천간합 렌즈를 시각적으로 분리** 렌더(검증서가 지적한 오독 위험 회피). CSS `.compat-*` 신설.
   - **콘텐츠 출처**: 멀티에이전트 워크플로(`saju-compat-content`) — 3개 독립 초안(명리정확/카피/간결 렌즈) → 적대 검증(명리 상생상극·천간합 방향·숙명론/공포/우열 표현 금지) → 병합. 3안 모두 myeongniCorrect·toneSafe 통과(8.5/8.5/9).
4. **검증**: 만세력 80/80 유지. `tests/verify-features.mjs`(Playwright) 11/11 통과(궁합 카드·자세히보기 토글·맨위로 노출/복귀/재숨김), 콘솔 에러 0.

### 라운드 1 — 신규 앱 0→1 구축
1. **리서치 워크플로(wq2fymeyx)** — KASI/uncle.tools/ytliu0 권위 출처에서 절기 시각·음양력 변환쌍·일주 앵커·KST 역사·e2e 검증벡터 수집 → `data/manse-reference.json`.
2. **만세력 엔진** — `astro.js`(율리우스일·태양황경·ΔT·절기솔버), `lunar.js`(삭·무중치윤 음양력), `saju.js`(4기둥·진태양시·자시경계), `constants.js`(명리 테이블).
   - 초기 저정밀 태양공식이 절기 시각 ~7분 오차 → **VSOP87 절단급수로 교체**해 분 단위 정확(오차 0분) 달성.
3. **분석/풀이** — `analyze.js`(오행·십성·7지표), `reading.js`(7섹션 자동풀이), `promptBuilder.js`(프롬프트 채우기).
4. **UI** — `index.html` + `styles/app.css`(seed 토큰 상속, 토스풍) + `app.js`(오케스트레이션·미니 마크다운 렌더러).
5. **검증** — `tests/manse.test.mjs` 70/70 통과. Playwright 렌더링 스크린샷 검증(콘솔 에러 0). 엣지케이스(자시·입춘경계·음력무효일·1955 UTC+8:30) 통과.

### 라운드 2 — 3차원 적대 검증 반영 (워크플로 w9j4qjtnd)
엔진·풀이충실도·디자인접근성 3개 에이전트가 독립 감사 → must-fix 11건 + 품질개선 반영:
- **엔진**: 진태양시 보정시각 표시 `Math.round→Math.floor`(분 오버플로 버그), 신강/신약 일간 자기지지 제외.
- **풀이 충실도**: 공망 일주+시주 旬 양쪽 판정(프롬프트 규약), 핵심유형 헤지 톤 추가, 6장 솔루션 십성 근거 반영, 스페셜 조합 4→7개 보강.
- **접근성(WCAG)**: 키보드 포커스 가시성(`:focus-visible` 분리), 색 대비 AA 통과(--ink-3/4/5·오행 텍스트색 격상), 세그먼트 `aria-pressed`·그룹 `aria-labelledby`, select `aria-label`, 뷰 전환 포커스 이동.
- **JS 버그**: 마크다운 표 separator 정규식(빈 셀 행 보존), `fallbackCopy` 에러 핸들링, 음력 소월(29일) 일자 상한(`daysInLunarMonth`).
- **반응형**: 360px 연도 select 텍스트 잘림 해결.
- 오탐 1건(TAEGEUK 순서 — 이미 표준 `['자','오']`), Input Rule 전제차이(위반 아님)는 스킵.
- 테스트 73/73 통과(소월 길이 3건 추가), 음력 윤달 UI 플로우 end-to-end 검증(에러 0).

### 라운드 3 — 대운/세운·이미지공유·디자인·배포 (4기능 추가)
1. **대운·세운** (`engine/luck.js`): 양남음녀 순행/역행, 절기(λ≡15 mod30) 기반 대운수, 대운 9구간·세운 12년. 결과에 타임라인/표 + 프롬프트 복사에 반영. 검증 7건 추가(손흥민 순행 대운수 10·첫대운 무신, 양녀 역행 병오, 세운 2026 병오=상관).
2. **이미지 저장·공유** (`share.js`): 외부 의존성 0, Canvas 2D로 1080×1350 사주 카드 직접 렌더(폰트 `document.fonts.ready` 대기). PNG 다운로드 + Web Share API(`navigator.canShare` 가드).
3. **디자인 다듬기**: 톱바 전폭 backdrop blur(내부 컨테이너 분리), 파비콘(인라인 SVG)·PWA/OG 메타, 대운/세운/공유 스타일, `prefers-reduced-motion`.
4. **배포 세팅**: `netlify.toml`·`.nojekyll`·`DEPLOY.md`(4가지 방법, saju 폴더만 — 모노레포 비공개 노출 금지 경고).
- 테스트 **80/80 통과**, 신기능 브라우저 검증(대운 현재 하이라이트·세운 올해·이미지 다운로드·에러 0).

### 라운드 4 — UX 개선 (공유/요약/용어/오행부족/맨위로)
1. **공유 전면 개편**: `공유하기`=모바일 OS 공유시트 직행(카톡·텔레그램 등)·PC는 자체 바텀시트(텔레그램·X·링크·카톡). `이미지 저장`=모바일 `navigator.share(파일)`로 갤러리 저장·앱 전송, PC는 다운로드. **공유 링크에 입력값 인코딩 → 링크로 결과 재현**(`applyParamsIfAny`).
2. **쉬운 요약 카드**(`buildSimpleSummary`)를 오행 분포 바로 아래 배치, 문구 간결화.
3. **부족한 오행 풀이**(`buildElementWeakness`+`ELEMENT_DETAIL`): 오행별 상징·약할 때 경향·장점·보완법 + 상생쌍(화생토 등) 복합설명. 아코디언.
4. **용어 팝오버**(`engine/glossary.js`): 원국·영성 잠재력·7대 지표·신강신약·대운/세운·진태양시 등 밑줄+ⓘ 탭 시 작은 설명창.
5. **한자 음독 병기**(지표명 등), **맨 위로 ↑ 버튼**(우하단, 스크롤 시 노출).
- 배포: GitHub `NJ1099/saju-ohaeng` → Vercel `sajumoya.vercel.app` 자동 재배포. 테스트 80/80 유지.

## 알려진 제약 / TODO
- **(라운드 6에서 해결)** ~~신강/신약 오행 세력 근사~~ → 통근·투간 기반 정밀 판정으로 교체됨. 다만 신강약은 유파별로 결론이 한 단계 달라지는 일이 흔해 화면에는 계속 "참고" 라벨을 붙인다.
- **격국은 내격만** — 투간 여부로 격을 잡는 유파, 종격·화격 등 외격은 판정하지 않음(`note` 명시).
- **용신은 항상 "후보"** — 정통 판정은 병약·통관·전왕까지 봐야 하므로 억부+조후 두 축으로만 제시.
- **음간의 양인 미판정** — 乙→寅설/乙→辰설이 갈려 제외.
- **공망**은 旬 기반 표준 계산이나 학파차로 "추가 확인 권장" 표기.
- 절기 정밀도는 1900~2100 최적(ΔT 다항식 범위). 그 밖 연도는 정밀도 저하 가능.
- 음력은 1900년대~2050년대 KASI 정합 범위에서 검증. 극단 연도 미검증.
- 마크다운 렌더러(app.js `md`)는 중첩 목록 미지원 — 단, 내장 풀이(reading.js)는 중첩 목록을 생성하지 않으므로 무영향.
- topbar `backdrop-filter`가 콘텐츠 컬럼(540px) 폭에만 적용 — 넓은 데스크탑 좌우 여백은 blur 미적용(저우선·모바일 우선이라 보류).

## 백업 / 배포
- **GitHub**: https://github.com/NJ1099/saju-ohaeng (독립 저장소, 모노레포와 분리) — 계정 NJ1099
- **Vercel**: https://sajumoya.vercel.app (GitHub 연결, **main push 시 자동 재배포**). vercel.json = `@vercel/static`(빌드 없음).
- **⚠️ 경로 변경됨 (2026-07-30 확인)** — 구 문서에 적혀 있던 `D:\Claude\saju`(개발 원본)와 `D:\saju-ohaeng`(배포용 사본)은 **둘 다 존재하지 않는다**. D 드라이브 자체가 없다.
- **현재 개발 원본**: `E:\AI\saju-ohaeng-main` (여기서 수정·테스트). 이 폴더는 `E:\AI` 모노레포(→ private `NJ1099/AI`)의 하위 폴더지만 **자체 `.git` 이 없다**.
- **배포용 클론**: `E:\saju-sync` (2026-07-30에 생성. seed 프로젝트의 `E:\seed-ledger-sync` 와 같은 패턴. 없어졌으면 `git clone https://github.com/NJ1099/saju-ohaeng.git E:/saju-sync` 로 다시 만들면 된다.)
- **배포 절차**:
  ```bash
  # ① 실제 내용이 다른 파일만 추린다 (줄바꿈 차이는 무시)
  cd /e/saju-sync && for f in $(git ls-files); do
    diff -q --strip-trailing-cr "$f" "/e/AI/saju-ohaeng-main/$f" >/dev/null 2>&1 || echo "$f"
  done
  # ② 텍스트만 LF → CRLF 변환하며 복사 (배포 저장소가 CRLF라 그냥 복사하면 전 파일이 diff에 잡힌다)
  awk '{ printf "%s\r\n", $0 }' "$f" > "/e/saju-sync/$f"
  # ③ ⚠️ 바이너리(assets/tarot/*.jpg)는 절대 awk 로 넘기지 말 것 — 파일이 깨진다. cp 로 복사.
  cp -r /e/AI/saju-ohaeng-main/assets /e/saju-sync/
  # ④ 커밋·푸시
  cd /e/saju-sync && npm test && git add -A && git commit -m "..." && git push origin main
  ```
  → Vercel 자동 재배포(1분 내). (git 자격증명은 Windows 자격증명관리자에 저장돼 push 자동 인증됨. 단 **클론 직후 `git config user.name/user.email` 설정 필요** — 전역 identity가 없어 첫 커밋이 거부된다.)
- **복사 전 반드시 역방향 diff 확인** — 배포 저장소에만 있는 변경을 덮어쓰지 않았는지 먼저 볼 것. (seed 프로젝트에서 통째 복사로 프로덕션 기능이 3일간 사라진 사고가 있었다.)
- 주의: 비공개 프로젝트가 많은 `E:\AI` 모노레포는 절대 공개 푸시 금지 — saju만 별도 저장소.
- **모노repo 백업 절차** — `cd /e/AI && git add -- saju-ohaeng-main` 처럼 **경로를 반드시 한정**할 것.
  `git add -A` 는 다른 프로젝트와 시크릿을 함께 끌고 온다. (라운드 8에서 루트 `.gitignore` 를 신설해
  `.env` 류는 차단되지만, 그래도 스테이징 목록을 눈으로 확인한 뒤 커밋하는 편이 안전하다.)
- **카톡 정식 공유**: `app.js`의 `KAKAO_JS_KEY` 비어 있음 → 카톡 버튼은 링크복사 폴백. 켜려면 카카오 developers JS키 + sajumoya.vercel.app 도메인 등록 후 키 입력.

## 다음에 시작할 때 체크리스트
1. 이 파일 먼저 읽기 — 특히 최상단 "지금 가장 먼저 볼 것"
2. `npm test` 로 엔진 회귀 확인 (80 + 273 + 2,886 + 2,840 = **6,079 기대**)
3. `node server.js` → http://localhost:4476/ 로 동작 확인
4. `npm run test:ui` 로 렌더링 회귀 확인 (Playwright 11 + 24 + 71 + 42 = **148**, 서버가 4476에 떠 있어야 함)
5. 배포: 위 "백업/배포"의 배포 절차(클론 → 동기화 → push)로 Vercel 자동 재배포. **이미지는 `cp`**
6. 작업 끝나면 이 HANDOFF.md 업데이트

## 라운드 8 이후 후보 (급하지 않음)
- **귀인 지도가 방장 브라우저에만 남는다** — 기기를 바꾸거나 브라우저 데이터를 지우면 사라진다.
  사용자가 이 한계를 알고 고른 설계다(서버·개인정보 보관 없음). 진짜 다중 참여 지도를 원하면
  Vercel Functions + Upstash Redis 가 필요하고, 그때는 **타인의 생년월일을 서버에 보관**하게 되므로
  개인정보처리방침을 그 구조에 맞게 다시 써야 한다(동의·보존기간·삭제 요청 창구).
- **귀인 지도에 궁합 점수 연동 안 됨** — `compat.js` 의 6축 100점은 지도에서 쓰지 않는다.
  의도적이다(관계에 점수를 붙이면 사람을 줄 세우게 된다). 넣으려면 톤 규약부터 다시 볼 것.
- **생년월일 유형은 일간·월지 두 글자만 본다** — 나머지 여섯 글자를 반영하지 않는다.
  정밀도를 올리려면 격국·용신 결과를 유형 문구에 섞는 방법이 있으나, "첫인상" 이라는 역할이 흐려진다.
- **`map.js` 는 자체 입력 폼을 갖는다** — 사주 폼(`app.js`)과 코드가 갈라져 있다.
  입력 UI 를 고칠 일이 생기면 두 곳을 함께 봐야 한다.

## 라운드 7 이후 후보 (급하지 않음)
- **타로 스프레드 확장** — 지금은 3장 고정. 켈틱 크로스(10장)·양자택일(5장) 등을 넣으려면
  `data/tarot-topics.json` 의 `positions` 배열 길이만 늘리면 엔진은 그대로 동작한다
  (`readSpread` 가 자리 수와 뽑은 장수를 대조해 검증한다). 단 UI 슬롯 그리드는 3열 고정이라 손봐야 한다.
- **카드 조합 해석** — 지금은 카드별 해석 + 스프레드 메타 관찰뿐. "특정 두 장이 같이 나오면"
  같은 조합 규칙은 미구현.
- **코트 카드(시종·기사·여왕·왕) 인물 해석** — 현재는 상황 해석으로만 쓴다.
  "주변의 어떤 사람"으로 읽는 유파는 반영 안 됨.
- **이미지 용량** — 78장 6.2MB(330px). 더 줄이려면 WebP 변환이 필요한데 Node 내장 인코더가
  없어 devDependency 추가가 필요하다. 현재 성능 문제는 없다(뽑힌 3장만 lazy 로드).

## 주요 파일 (빠른 참조)
| 파일 | 역할 |
|------|------|
| `index.html` / `app.js` / `styles/app.css` | SPA 셸·메뉴·사주 오케스트레이션·토스풍 디자인 |
| `router.js` | 해시 라우팅 + `body[data-route]` 테마 스코프. **라운드 8에서 `#/map`·`#/terms`·`#/privacy` 추가** |
| `map.js` | **라운드 8** 귀인 지도 UI(지도 만들기 → 참여 → 판정 → 누적) · localStorage 저장 |
| `engine/gwiin.js` | **라운드 8** 귀인 관계 5유형 판정(십성 기준선 + 오행보완·합충·천을귀인 보정) |
| `engine/persona.js` | **라운드 8** 생년월일 유형(일간 물상 10 × 계절 4 = 40조합) |
| `tests/persona.test.mjs` | **라운드 8** 유형·귀인 2,840개 단위테스트(40조합 전량·유형 분포 상하한·톤 규약) |
| `tests/verify-map.mjs` | **라운드 8** Playwright 지도·유형·약관 검증 42건 |
| `tarot.js` / `styles/tarot.css` | **라운드 7** 타로 UI(주제→펼침→선택→공개→해석) · 다크 테마 |
| `engine/tarot.js` | **라운드 7** 덱·셔플·드로우·스프레드 해석·공유 인코딩 (순수 로직) |
| `data/tarot/*.json` | **라운드 7** 78장 해석(9주제 × 정/역 = 1,404줄) — 수트별 5파일 |
| `data/tarot-topics.json` | **라운드 7** 9주제 정의 + 주제별 3자리 뜻 |
| `data/tarot-prompt-template.txt` | **라운드 7** 타로 전용 LLM 프롬프트 |
| `assets/tarot/*.jpg` | **라운드 7** 라이더-웨이트 78장(퍼블릭 도메인, 330px, 6.2MB) |
| `scripts/fetch-tarot-images.mjs` | **라운드 7** 이미지 재수집 스크립트(평소엔 실행할 일 없음) |
| `engine/constants.js` | 명리 표준 테이블(천간·지지·오행·지장간·공망·삼합·오호둔·오서둔·태극귀인·십성) |
| `engine/astro.js` | 율리우스일·VSOP87 태양황경·ΔT·24절기 솔버 |
| `engine/lunar.js` | 음↔양력 변환(Meeus 삭·무중치윤 윤달) |
| `engine/saju.js` | **핵심** 4기둥 산출(년주 입춘·월주 절기·일주 JDN·시주 오서둔·진태양시) |
| `engine/analyze.js` | 오행 분포·십성·7대 영성지표·영성점수 |
| `engine/theory.js` | **라운드 6** 통근·신강약 / 십이운성 / 신살 / 조후 / 격국 / 용신 / 형충회합 (`analyzeTheory`가 집계) |
| `engine/compat.js` | **라운드 6** 두 사람 궁합 6축 100점 (`analyzeCompatibility(A,B)`) |
| `engine/luck.js` | 대운(순행/역행·대운수)·세운 계산 |
| `engine/reading.js` | 내장 자동풀이 + 쉬운 요약(`buildSimpleSummary`) + 부족한 오행 풀이(`buildElementWeakness`) + 잘 맞는 사주 힌트(`buildCompatibility`) |
| `engine/glossary.js` | 용어 설명 팝오버 사전 |
| `engine/promptBuilder.js` | 원국(+대운/세운) 채운 LLM 프롬프트 생성 — 단독·궁합 2종 |
| `share.js` | Canvas 사주 카드 이미지 생성(저장·공유) |
| `vercel.json`(사본) / `DEPLOY.md` / `netlify.toml` / `.nojekyll` | 배포 설정·가이드 |
| `THEORY.md` | **라운드 6** 명리 이론 근거·채택 학설·의도적 제외 항목·참고 문헌 |
| `data/prompt-template.txt` | 원본 프롬프트 본문(브런치 댓글 제외) |
| `data/compat-prompt-template.txt` | **라운드 6** 궁합 전용 LLM 프롬프트 |
| `data/manse-reference.json` | 검증 레퍼런스(절기·음양력·일주앵커·e2e·KST역사) |
| `data/theory-reference.json` | **라운드 6** 이론 검증 벡터(unseong·sinsal·relations·johu·gyeokguk·compat) |
| `server.js` | 무의존 정적 서버(포트 4476) |
| `tests/manse.test.mjs` | 만세력 80개 단위테스트(절기·음양력·일주·e2e·소월·대운) |
| `tests/theory.test.mjs` | **라운드 6** 이론·궁합 273개 단위테스트 |
| `tests/tarot.test.mjs` | **라운드 7** 타로 2,886개 단위테스트(덱·해석 전량·드로우 분포·URL 왕복) |
| `tests/verify-features.mjs` / `tests/verify-compat.mjs` | Playwright UI 검증 (11 + 24건) |
| `tests/verify-tarot.mjs` | **라운드 7** Playwright 타로 UI 검증 56건 |
| `tests/screenshot.mjs` | Playwright 렌더링 스크린샷 |

## 핵심 설계 메모
- **분리 원칙**: 년/월주는 출생의 **물리적 순간(UT 태양황경)** 으로, 시주/일주 경계는 **진태양시 보정 클럭**으로 계산 — 진태양시는 시계 라벨링 문제이지 천체 순간을 바꾸지 않음.
- **일주 앵커**: `((JDN-11) mod 60)`, idx0=갑자. 2000-01-07=갑자로 검증(흔한 오류값 "1984-02-02=갑자"는 실제 병인 — 사용 금지).
- **월지 산출**: `floor((태양황경-315)/30)` 섹터로 寅~丑 직접 결정(절기 시각 일일이 안 구해도 됨).
- **프롬프트 호환**: 앱이 원국을 정확히 계산해 LLM엔 4기둥만 넘김 → 프롬프트의 "생년월일 임의계산 금지" 규칙과 완전 호환.
- **명리 이론의 채택 근거·학설차·의도적 제외 항목은 [`THEORY.md`](./THEORY.md) 에 전부 정리돼 있다.** 이론 판정을 고치기 전에 반드시 읽을 것 — 대부분의 "틀린 것 같은 값"은 학설차이며 이미 문서에 근거가 적혀 있다.
